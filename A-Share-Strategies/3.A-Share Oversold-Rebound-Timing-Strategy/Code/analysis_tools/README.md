# Analysis Tools

This package keeps the source-side tool implementations used to generate factor-analysis and attribution-analysis outputs. The presentation-ready outputs themselves are packaged under `Factor Analysis` and `Attribution Analysis` at the project root.
