# Strategy Library

This package hosts optional strategy-specific hooks. The active package keeps a minimal custom strategy module that delegates ranking to the framework default while preserving a dedicated extension point for future strategy-specific filters or scoring logic.
